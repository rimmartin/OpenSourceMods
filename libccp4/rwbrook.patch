# This patch file was generated by NetBeans IDE
# It uses platform neutral UTF-8 encoding and \n newlines.
--- a/rwbrook
+++ b/rwbrook
@@ -2598,10 +2598,11 @@
 C     .. Scalar Arguments ..
       REAL C
       INTEGER IELEC,Ifail,IWT,NG
-      CHARACTER ID*4,IDCHK*4,STRING*200
+      CHARACTER ID*4,IDCHK*4,STRING*200,CLIBDIR*32767,CLIBFILE*265
+      CHARACTER*32767 LOGNAM
 C     ..
 C     .. Array Arguments ..
-      REAL A(4),B(4),CU(2),MO(2)
+      REAL A(NG),B(NG),CU(2),MO(2)
 C     ..
 C     .. Local Scalars ..
       INTEGER NGauss,IOS
@@ -2629,8 +2630,13 @@
       IF (IOS .NE. 0) CALL CCPERR(1,'Error opening ATOMSF file')
 
       IF (.NOT.OP) THEN
+        CALL getenv("CLIBD", CLIBDIR)
+        CLIBDIR=TRIM(ADJUSTL(CLIBDIR))
+        CALL getenv("CLIBFILE", CLIBFILE)
+        CLIBFILE=TRIM(ADJUSTL(CLIBFILE))
+        LOGNAM = TRIM(CLIBDIR)//'/'//TRIM(CLIBFILE)
         Ifail  = 1
-        CALL CCPDPN(45,'ATOMSF','READONLY','F',0,Ifail)
+        CALL CCPDPN(45, LOGNAM,'READONLY','F',0,Ifail)
         IF (Ifail.LT.0) CALL CCPERR(1,'Error opening library file')
       ELSE
         REWIND 45
@@ -2695,7 +2701,13 @@
 C
    60 READ (45,FMT=6006) IWT,IELEC,C
       READ (45,FMT=6008) A(1),A(2),A(3),A(4)
+      IF (NG .EQ. 5) THEN
+        READ (45,FMT=6008) A(5)
+      ENDIF
       READ (45,FMT=6008) B(1),B(2),B(3),B(4)
+      IF (NG .EQ. 5) THEN
+        READ (45,FMT=6008) B(5)
+      ENDIF
       READ (45,FMT=6008) CU(1),CU(2),MO(1),MO(2)
       Ifail = 0
 C
